[package]
name = "knudge-mcp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Servidor MCP do knudge (reativo a comportamento)."

[lib]
name = "knudge_mcp"
path = "src/lib.rs"

[dependencies]
knudge-core = { path = "../knudge-core" }

[lints]
workspace = true
